---
description: Open the character editor (捏人界面) in the Aria desktop app
allowed-tools: Bash
---

Run this single command via the Bash tool, then reply with only the command's output verbatim and nothing else:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/model.sh"
```
